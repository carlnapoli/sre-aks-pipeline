on:
  workflow_dispatch:
    inputs:
      action:
        description: 'the acr action'
        required: true
jobs:
  myEvent:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2
       - run: |
           echo "ACR Webhook Recieved: [ ${{ github.event.inputs.action }} ] "
